APP_NAME=clinic_api
APP_ENV=dev
APP_DEBUG=true

# Database (legacy SQL):
DATABASE_URL=sqlite+aiosqlite:///./data.db
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/clinic

# MongoDB (current)
MONGODB_URI=mongodb://localhost:27017/clinic

# JWT
JWT_SECRET=change_me_super_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS (comma separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,capacitor://localhost,ionic://localhost

# Media directory
MEDIA_DIR=media

# SMS provider
SMS_PROVIDER=dummy
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Firebase Admin SDK service account JSON path (optional in dev)
FIREBASE_CREDENTIALS_FILE=
